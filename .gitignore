venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/
.env

# ML artefacts are intentionally committed so the grader/reviewer
# can inspect results without re-running the full pipeline.
# Remove these lines only if models grow beyond GitHub's 100 MB limit
# (use Git LFS in that case: git lfs track "*.joblib").
# ml/models/
# ml/results/
# data/auth_logs_labeled.csv